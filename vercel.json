{
  "buildCommand": "node setup-env.js && npm run build",
  "outputDirectory": "dist/ai-chat-app",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
